{
  "scripts": {
    "start": "NODE_PATH=/usr/lib/node_modules/ node --experimental-fetch restServer.js ../rwa_lpejakovi20_conf.csv",
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "tsc --watch false"
  },
  "dependencies": {
    "node-fetch": "^3.3.0"
  }
}
